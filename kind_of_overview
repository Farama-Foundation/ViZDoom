python <- c++ api <- ViziaDoomController <- zdoom <- (scenario + map)

c++ api:
	void init(???);
	int * get_state_format();
	int get_action_format();

	void new_episode();
	float make_action( int const* action);
	int is_finished();
	float get_summary_reward();
	float * get_image_state();
	float * get_misc_state();

zdoom configurable stuff:
	- hud visibility (default off)
	- weapon visibility (default off)

zdoom stuff to delete:
	- sound configuration
	- menu, ~console and other meta buttons
	- ?console output

scenario configurable stuff:
	- living reward
	- rewarded actions 
	- timeout in frames
	- abailable ammo
	- available keys to press
	- weapon
	- available weapons ???
	- resolution
	- image format:
		- raw (byte)
		- float from byte
		- float from byte in <0,1>
		- rgb float in <0,1>, shape = (3, y, x)
		- greyscale float in <0,1>
	- additional parameters to be included in state e.g.:
		- ammo
		- hp
		- time left
	