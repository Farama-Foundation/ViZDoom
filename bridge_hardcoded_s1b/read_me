#Kompilacja silnika
cmake . -DCMAKE_BUILD_TYPE=Release
make



Przed kompilacją:
W folderze muszą znajdować się: zawartość tego folderu, doom2.wad, s1_b.wad, 
ViziaDoomController.h, ViziaDoomController.cpp, cały folder src/boost 

#Kompilacja api.cpp
python setup.py build_ext --inplace -I .

Powinno przejść.

Użycie w pythonie:

import api
api.#nazwafunkcji z Bridge.h 			(np api.init(640,480,1000)_

Funkcje:

VIZIA_StateFormat* get_state_format(); - działa jak poprzednio
VIZIA_State* get_state(); - działa jak poprzednio

int get_action_format(); - działa jak poprzednio
void new_episode(); 			- działa
double make_action( int const* action ); -make_action([0,1,1]) (Left, right, shoot) 0 - off, 1 -on
int is_finished();
void init(int x, int y, int maxtime); (szerokość, wysokość, max czas trwania scenariusza po którym się wyłączy (dajcie z 1000), nie wiem czy zeruje się czas po odpaleniu new_episode())
void close(); 				-zamyka silnik dooma. 
double get_summary_reward();  





